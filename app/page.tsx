import { fetchFunc } from '@/fetchFunc/fetchapi'
import styles from './page.module.css'

export default async function Home() {
  // let data = await fetchFunc("search/?q=despacito&hl=en&gl=US");
  // console.log(data);
  return (
    <>
      Live Server
    </>
  )
}
